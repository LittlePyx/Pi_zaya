# Legacy Archives

These files are kept for historical context only.

The active product path is FastAPI + React:

- Frontend: `web/src/main.tsx`
- Backend: `api/main.py`
- Local app: `http://127.0.0.1:5173/`
- Local API: `http://127.0.0.1:8000/`

Archive areas:

- `streamlit/`: old Streamlit UI notes, runtime patches, and legacy dependency pins.
- `plans/`: superseded reviews, roadmaps, and implementation plans.

Do not use this directory as implementation guidance for new product work.
