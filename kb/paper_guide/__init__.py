"""Paper Guide package.

This package exists to provide stable import paths during the ongoing migration
from the legacy flat-module layout (e.g. ``kb/paper_guide_*.py``) to a more
structured package layout (e.g. ``kb/paper_guide/*.py``).
"""

